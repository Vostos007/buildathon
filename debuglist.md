# Smart Salary Slicer - TODO Checklist & Code Review
**Агент:** AI Debug Agent
**Последнее обновление:** 2026-01-29
**Статус проекта:** 🚧 В разработке (бизнес-логика готова, UI в работе)

---

## 📋 Часть 1: TODO Checklist (Что нужно создать)

### 🏗️ Базовая инфраструктура
Это фундамент приложения - без этого нельзя двигаться дальше.

- [ ] **Создать `src/main.tsx`**
  - Импортировать App компонент
  - Монтировать в root div
  - Подключить глобальные стили

- [ ] **Создать `src/App.tsx`**
  - Импортировать useBudget hook
  - Импортировать useBudgetCalculations hook
  - Структура компонента с секциями

- [ ] **Создать `src/globals.css`**
  - Tailwind directives (@tailwind base, @tailwind components, @tailwind utilities)
  - Dark mode base styles
  - Recharts theme customization
  - Custom animations (fade-in, slide-up, pulse-slow)

- [ ] **Создать папку `src/components/`**
  - Подпапки: layout/, ui/, charts/

### 🎨 Layout компоненты
Визуальный каркас приложения.

- [ ] **Создать `src/components/layout/Layout.tsx`**
  - Props: children
  - Dark mode class на html/body
  - Max-width контейнер
  - Padding и spacing

- [ ] **Создать `src/components/layout/Header.tsx`**
  - App name/logo
  - Краткое описание
  - Возможно: кнопка Reset to Default

### 🧩 UI компоненты
Интерактивные элементы для управления бюджетом.

- [ ] **Создать `src/components/ui/IncomeInput.tsx`**
  - Props: IncomeInputProps (value, onChange)
  - Большой центрированный input (text-4xl)
  - Форматирование с thousand separators
  - Currency prefix ($)
  - Auto-focus при mount
  - Валидация (0 - 999,999,999)

- [ ] **Создать `src/components/ui/CategoryCard.tsx`**
  - Props: CategoryCardProps (category, percentage, amount, color)
  - Иконка из lucide-react
  - Category label
  - Percentage badge
  - Форматированная сумма
  - Hover эффект (translateY + shadow)
  - Active state для chart highlighting

- [ ] **Создать `src/components/ui/DailySafeToSpendCard.tsx`**
  - Props: DailySafeToSpendProps (dailyAmount)
  - Gold accent (для trust/важности)
  - Иконка (Calendar или Clock)
  - Формула: Wants / 30
  - Анимированное появление
  - Hover эффект

- [ ] **Создать `src/components/ui/PercentageSlider.tsx`**
  - Props: PercentageSliderProps (category, value, onChange, color)
  - Radix UI Slider (для accessibility)
  - Цветной трек (matching category)
  - Real-time percentage display
  - Min/max labels (0%, 100%)
  - Keyboard navigation (arrow keys)
  - Debounced updates (для производительности)

### 📊 Charts визуализация
Визуализация данных распределения бюджета.

- [ ] **Создать `src/components/charts/BudgetDonutChart.tsx`**
  - Props: BudgetDonutChartProps (data, activeCategory, onCategoryHover)
  - Recharts PieChart + Pie
  - Donut effect (innerRadius 60%, outerRadius 80%)
  - Custom Tooltip (с процентом и суммой)
  - Active shape highlight (увеличение на 10% при hover)
  - Smooth animations (500ms)
  - Responsive sizing (ResponsiveContainer)
  - Цвета: Needs (violet), Wants (green), Savings (cyan)

### 🔧 Интеграция и сборка
Соединение всех частей.

- [ ] **Обновить `src/App.tsx` с полным UI**
  - Разместить все компоненты в правильном порядке
  - Прокинуть state через props
  - Обработать все events (onChange, onHover)
  - Test state flow

- [ ] **Создать `tsconfig.json`** (если отсутствует)
  - Добавить path aliases из vite.config.ts
  - Подтвердить baseUrl: "."
  - Маппинги: @/, @components/*, @hooks/*, @state/*, @types/*, @utils/*, @constants/*

### 📚 Документация
- [ ] **Создать `README.md`**
  - Project description (50/30/20 rule)
  - Installation (npm install)
  - Development (npm run dev)
  - Build (npm run build)
  - Tech stack (React 18, TypeScript, Tailwind, Recharts, Radix UI, Lucide)
  - Project structure
  - Screenshots/примеры

---

## 🔍 Часть 2: Code Review Issues (Что требует улучшения)

Это проблемы в существующем коде - не блокируют разработку, но лучше исправить.

### ⚠️ [HIGH] Дублирование логики валидации
**Файлы:**
- `src/state/budgetReducer.ts` - содержит `validateAndAdjustPercentages` (строки 14-95)
- `src/utils/percentageValidator.ts` - содержит `adjustPercentages` (строки 96-175)

**Описание:** Одна и та же логика реализована в двух местах.

**Проблема:**
- Нарушение DRY (Don't Repeat Yourself)
- Потенциальная рассинхронизация при изменениях
- Сложно поддерживать два одинаковых куска кода

**Решение:**
Выбрать один файл для логики валидации и удалить другой:
- **Вариант 1:** Удалить `validateAndAdjustPercentages` из budgetReducer.ts, использовать `adjustPercentages` из percentageValidator.ts
- **Вариант 2:** Удалить `adjustPercentages` из percentageValidator.ts, использовать `validateAndAdjustPercentages` из budgetReducer.ts

**Приоритет:** 🟡 Высокий
**Статус:** ❌ Не исправлено

---

### ⚠️ [HIGH] Неконсистентность шрифтов
**Файлы:**
- `tailwind.config.ts` (строка 55): использует 'Plus Jakarta Sans'
- `index.html` (строка 12): подключает 'Plus Jakarta Sans'
- План спецификации / UI UX Pro Max: рекомендует 'IBM Plex Sans'

**Текущий код:**
```typescript
// tailwind.config.ts:55
sans: ['Plus Jakarta Sans', 'system-ui', '-apple-system', 'sans-serif'],
```

**Вопрос для разработчика:** Какой шрифт использовать?
- **Вариант A:** Plus Jakarta Sans (текущий) - modern, clean
- **Вариант B:** IBM Plex Sans (из плана) - professional, fintech-feel

**Решение:** Выбрать и применить согласованно во всех файлах.

**Приоритет:** 🟡 Высокий
**Статус:** ❌ Не исправлено

---

### ⚠️ [HIGH] Fallback логика с жесткими коэффициентами
**Файл:** `src/state/budgetReducer.ts`
**Строки:** 80-89
**Описание:** Fallback использует жесткие 0.5, 0.3, 0.2 вместо пропорционального распределения.

**Проблемный код:**
```typescript
// budgetReducer.ts:84-86
proportional: Percentages = {
  needs: changedCategory === 'needs' ? clampedValue : remaining * 0.5,
  wants: changedCategory === 'wants' ? clampedValue : remaining * 0.3,
  savings: changedCategory === 'savings' ? clampedValue : remaining * 0.2,
};
```

**Проблема:**
- Жесткие коэффициенты не учитывают текущее распределение
- Нарушает принцип пропорциональности
- При fallback не-измененная категория получает фиксированный % (0.5 или 0.3 от remaining)

**Решение:**
Использовать `distributeProportionally` из `percentageValidator.ts` вместо fallback с жесткими коэффициентами.

**Приоритет:** 🟡 Высокий
**Статус:** ❌ Не исправлено

---

### 🟡 [MEDIUM] Отсутствуют TypeScript path aliases
**Файл:** `tsconfig.json`
**Описание:** Vite config настроен с aliases, но tsconfig.json их не подтверждает.

**Влияние:**
- TypeScript autocomplete может не работать
- Импорты с @ могут подчеркиваться красным
- "Go to definition" может не находить файлы

**Решение:** Добавить в tsconfig.json:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@hooks/*": ["./src/hooks/*"],
      "@state/*": ["./src/state/*"],
      "@types/*": ["./src/types/*"],
      "@utils/*": ["./src/utils/*"],
      "@constants/*": ["./src/constants/*"],
      "@styles/*": ["./src/styles/*"]
    }
  }
}
```

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

### 🟡 [MEDIUM] Иконки как строки вместо компонентов
**Файл:** `src/constants/budget.ts`
**Строки:** 49, 60, 71
**Описание:** `CATEGORY_CONFIG.icon` указан как строка, но lucide-react экспортирует компоненты.

**Текущий код:**
```typescript
// budget.ts:49, 60, 71
icon: 'Home',         // Строка!
icon: 'ShoppingBag',  // Строка!
icon: 'PiggyBank',   // Строка!
```

**Проблема:** UI компоненты не смогут использовать эти иконки.

**Решение 1 (хранить функции):**
```typescript
// constants/budget.ts
import { Home, ShoppingBag, PiggyBank } from 'lucide-react';

export const CATEGORY_CONFIG = {
  needs: {
    // ...
    icon: Home,  // Компонент!
  },
  // ...
}
```

**Решение 2 (хранить строки, маппить в компонентах):**
```typescript
// Оставить как есть, мапить в компонентах:
const iconMap = {
  Home: HomeIcon,
  ShoppingBag: ShoppingBagIcon,
  PiggyBank: PiggyBankIcon,
};
const Icon = iconMap[CATEGORY_CONFIG[category].icon];
```

**Решение 1 лучше** (более type-safe).

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

### 🟡 [MEDIUM] Отсутствует обработка отрицательных чисел в input
**Файл:** `src/utils/formatters.ts`
**Строки:** 73-89
**Описание:** `formatInputValue` не валидирует отрицательные значения.

**Проблемные edge cases:**
1. Пользователь вводит "-" → очистится, вернется пустая строка
2. Пользователь вводит "-1000" → отрицательное число пройдет
3. Нет проверки на max income (999,999,999 из INPUT_CONFIG)

**Текущий код:**
```typescript
// formatters.ts:81
const num = parseFloat(cleaned);
if (isNaN(num)) return '';
```

**Решение:**
```typescript
const num = parseFloat(cleaned);
if (isNaN(num) || num < 0) return ''; // Блокировать отрицательные
// Или clamp к диапазону
const clamped = Math.max(0, Math.min(INPUT_CONFIG.maxIncome, num));
```

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

### 🟡 [MEDIUM] Цвет 'wants' категории
**Файл:** `src/constants/budget.ts`
**Строки:** 22-26
**Описание:** Используется `#22C55E` (green-600) для wants.

**Вопрос:** Соответствует ли это дизайн-системе?
- Green-600 (#22C55E) - темный зеленый
- Green-500 (#10B981) - средний зеленый (лучший контраст на dark background)
- UI UX Pro Max рекомендует gold (#F59E0B) для accent/trust элементов

**Текущий:**
```typescript
// budget.ts:23
primary: '#22C55E',  // Green-600
```

**Рекомендация:** Рассмотреть green-500 (#10B981) или gold (#F59E0B) в зависимости от выбранной дизайн-системы.

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

### 🔵 [LOW] Отсутствует CSS для анимаций
**Файл:** `src/globals.css` - отсутствует
**Описание:** Tailwind config определяет keyframes (строки 81-94), но без globals.css они не будут работать.

**Что нужно в CSS:**
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Dark mode base styles */
html.dark {
  background-color: #0A0A0F;
  color: #FFFFFF;
}

/* Recharts theme */
.recharts-tooltip-wrapper {
  background-color: #1A1A25;
  border: 1px solid #3F3F46;
  border-radius: 0.5rem;
}

/* Scrollbar customization */
::-webkit-scrollbar {
  width: 8px;
}
::-webkit-scrollbar-track {
  background: #12121A;
}
::-webkit-scrollbar-thumb {
  background: #3F3F46;
  border-radius: 4px;
}
```

**Приоритет:** 🔵 Низкий
**Статус:** ❌ Не исправлено

---

## 🐛 Часть 3: Баги из UI (по скриншотам)

Замечания по реальному отображению приложения в интерфейсе.

### 🔴 [HIGH] Итог процентов показывается как 99.99999999999997% вместо 100%
**Файл:** `src/App.tsx`
**Строки:** 86-98
**Описание:** В блоке «Adjust Percentages» внизу отображается «Total: 99.99999999999997%» (красным). Причина — сложение дробных процентов в JavaScript даёт погрешность плавающей точки, а не ровно 100.

**Текущий код:**
```typescript
// App.tsx:90-95
Total: {' '}
<span className={...}>
  {Object.values(state.percentages).reduce((a, b) => a + b, 0)}%
</span>
```
И проверка `=== 100` никогда не срабатывает при float, поэтому итог всегда красный.

**Что сделать:**
1. При отображении округлять сумму до 1–2 знаков: например `Math.round(sum * 100) / 100` или `Number((a + b + c).toFixed(1))`, и показывать «100%», когда по факту 100.
2. Для цвета использовать проверку «почти 100»: `Math.abs(sum - 100) < 0.01` вместо `=== 100`.

**Пример исправления:**
```typescript
const totalPercent = Object.values(state.percentages).reduce((a, b) => a + b, 0);
const displayPercent = Math.abs(totalPercent - 100) < 0.01 ? 100 : Math.round(totalPercent * 100) / 100;
const isValid = Math.abs(totalPercent - 100) < 0.01;

<span className={`... ${isValid ? 'text-wants' : 'text-red-500'}`}>
  {displayPercent}%
</span>
```

**Приоритет:** 🔴 Высокий
**Статус:** ❌ Не исправлено

---

### 🔴 [HIGH] Слишком много знаков после запятой в суммах
**Файлы:** `src/utils/formatters.ts`, все места вызова `formatCurrency`
**Описание:** Везде суммы выводятся с двумя десятичными знаками ($1,440.00, $48.00, $1,114.29). Для целых сумм и для «дневной траты» лучше показывать без копеек или с одним знаком (например $1,440 или $48.00 по желанию).

**Текущий код (formatters.ts:10-21):**
```typescript
export function formatCurrency(amount: number, currency = 'USD', locale = 'en-US'): string {
  return new Intl.NumberFormat(locale, {
    style: 'currency',
    currency,
    minimumFractionDigits: 2,  // всегда .00
    maximumFractionDigits: 2,
  }).format(amount);
}
```

**Что сделать:**
1. Либо добавить параметр `decimals?: number` в `formatCurrency` и по умолчанию использовать 0 для «круглых» сумм или 2 для точных.
2. Либо ввести отдельную функцию для «короткого» отображения: целые доллары без копеек, когда `amount === Math.round(amount)`.
3. Единообразно: карточки категорий и центр доната — без лишних .00 для целых; Daily Safe-to-Spend можно оставить с 2 знаками или тоже сократить.

**Пример (опция с параметром):**
```typescript
export function formatCurrency(
  amount: number,
  currency: string = 'USD',
  locale: string = 'en-US',
  decimals: number = 2
): string {
  return new Intl.NumberFormat(locale, {
    style: 'currency',
    currency,
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  }).format(amount);
}
// Вызов для целых: formatCurrency(amount, 'USD', 'en-US', 0) когда amount % 1 === 0
```

**Приоритет:** 🔴 Высокий
**Статус:** ❌ Не исправлено

---

### 🟡 [MEDIUM] Проблемы с элементами интерфейса
**Описание (по скриншотам):**
1. **Центр доната и тултип:** В центре доната должен быть только «Total» и общая сумма. При наведении на сектор показывается тултип с категорией и суммой — убедиться, что центр не перекрывается тултипом и не подменяет «Total» данными категории.
2. **Соответствие секторов и карточек:** Проверить, что цвета и порядок секторов доната совпадают с карточками: Needs — фиолетовый, Wants — зелёный, Savings — голубой. Порядок в `chartData` должен совпадать с визуальным порядком.
3. **Иконки и подписи:** Убедиться, что в карточках категорий и в слайдерах одни и те же иконки и подписи (Needs = дом, Wants = сумка, Savings = копилка), без путаницы.

**Где смотреть:**
- `src/components/charts/BudgetDonutChart.tsx` — центр (Total), тултип, порядок/цвета `data`.
- `src/constants/budget.ts` — цвета категорий.
- `src/hooks/useBudgetCalculations.ts` — порядок `chartData` (CATEGORY_TYPES).
- Карточки и слайдеры — единые label/icon из CATEGORY_CONFIG.

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

### 🟡 [UX] Убрать «Hover over a segment» и сделать панель у доната продвинутой
**Файл:** `src/components/charts/BudgetDonutChart.tsx`
**Описание:** Справа от доната сейчас показывается серая плашка с текстом «Hover over a segment», когда ни один сектор не наведён. Выглядит как временный placeholder и портит впечатление от интерфейса.

**Что сделать:**
1. **Убрать «убогий» placeholder** — не показывать блок с текстом «Hover over a segment», когда нет наведения.
2. **Сделать продвинутый UI/UX** для панели рядом с донатом:
   - **Вариант A:** Когда ничего не наведено — либо пустая область (без текста), либо компактная легенда: три строки «Needs 50% · $2,500» / «Wants 30% · $1,500» / «Savings 20% · $1,000» в том же стиле, что карточки (иконка + цвет + сумма). При наведении на сектор — подсветить соответствующую строку и при желании показать доп. детали.
   - **Вариант B:** Всегда показывать мини-сводку по трём категориям (название, %, сумма) в виде аккуратного списка/карточек; при hover по сектору — выделять нужную строку и добавлять лёгкую анимацию (scale, glow).
   - **Вариант C:** Своё решение в духе neo-bank: без «Hover over a segment», без пустой серой коробки; либо элегантная легенда, либо контекстная подсказка только при наведении (но не внутри круга и без перекрытия Total).
3. Стиль: тёмная тема, те же цвета категорий, скругления, типографика; без «инструкций» в лоб, максимум — лаконичные подписи/цифры.

**Где менять:** `BudgetDonutChart.tsx` — компонент `ChartTooltipPanel`: убрать ветку с «Hover over a segment», заменить на выбранный продвинутый вариант (легенда / мини-сводка / контекстная подсказка снаружи).

**Приоритет:** 🟡 Средний (UX)
**Статус:** ❌ Не исправлено

---

## 🎯 Часть 4: Feature Requests (Новая функциональность)

### 🔴 [FEATURE] Независимые ползунки и поле «Available to distribute»
**Описание:** Сейчас при изменении одного слайдера остальные автоматически перераспределяются, чтобы сумма оставалась 100%. Нужно сделать **независимые** ползунки в рамках 100%: пользователь сам решает, куда распределить проценты.

**Требования:**
1. **Независимые слайдеры:** Изменение одного слайдера **не** меняет другие. Каждый слайдер только задаёт процент своей категории (0–100%).
2. **Поле «Available to distribute»:** Когда сумма трёх категорий **меньше 100%**, показывать блок (на английском, в том же стиле):
   - **«Available to distribute: X%»** — где X = 100 − (Needs + Wants + Savings).
   - Стиль: тёмная тема, тот же шрифт и отступы, что и у блока «Total» / «Adjust Percentages». Можно выделить цветом (например, нейтральный или зелёный).
3. **Когда сумма больше 100%:** Показывать, например, **«Over by X%»** (красным), чтобы пользователь видел, что нужно уменьшить общую сумму.
4. **Когда сумма ровно 100%:** Показывать **«Total: 100%»** (как сейчас, зелёным). Округлять с учётом float (см. Часть 3 — баг с 99.99999999999997%).

**Где менять:**

| Файл | Изменения |
|------|-----------|
| `src/state/budgetReducer.ts` | В `SET_PERCENTAGE`: **не вызывать** `validateAndAdjustPercentages`. Просто установить `percentages[category] = clamp(0, 100, value)` и вернуть новый state. Остальные категории не трогать. |
| `src/hooks/useBudgetCalculations.ts` | Добавить в возвращаемый объект: `availableToDistribute: 100 - totalPercentage` (или отдельно `totalPercentage` уже есть — использовать его). |
| `src/App.tsx` | 1) Вывести `availableToDistribute` из хука. 2) Под блоком слайдеров, над/рядом с «Total», показывать: если `availableToDistribute > 0` — **«Available to distribute: X%»**; если `< 0` — **«Over by X%»**; если `=== 0` (с учётом округления) — **«Total: 100%»**. 3) Для отображения X использовать округление (1 знак или целое), чтобы не было 9.99999999999999. |

**Текст на английском (как в интерфейсе):**
- `Available to distribute: 10%` — когда осталось 10% к распределению.
- `Over by 5%` — когда сумма процентов превышает 100% на 5%.
- `Total: 100%` — когда распределено ровно 100%.

**Стиль:** Тот же, что у текущего «Total» (шрифт, размер, цвет фона/рамки). Для «Available to distribute» — нейтральный или зелёный акцент; для «Over by» — красный.

**Приоритет:** 🔴 Высокий (feature)
**Статус:** ❌ Не реализовано

---

## 🚀 Часть 5: Real Life Features (После хакатона)

Фичи, которые делают Salary Slicer полезным в реальной жизни и удобным для развития после демо.

### 1. Функциональность

#### ⚡️ [FEATURE] Режимы жизни (Presets)
**Описание:** Сейчас правило 50/30/20 жёсткое. В жизни у всех разные ситуации. Добавить кнопки-пресеты над графиком, по клику — анимированно выставлять проценты.

**Пресеты (примеры):**
| Режим | Needs | Wants | Savings | Описание |
|-------|--------|--------|---------|----------|
| **Student** | 70% | 10% | 20% | Общежитие/еда, минимум развлечений |
| **Balanced** | 50% | 30% | 20% | Классика 50/30/20 |
| **Aggressive Saver (Fire Mode)** | 30% | 0% | 70% | Экстремальная экономия, максимум в инвестиции |
| **Family** | 60% | 25% | 15% | Свой вариант под семью |

**Что сделать:**
- Над графиком (или рядом с «Reset to 50/30/20») добавить кнопки: «Student», «Balanced», «Aggressive Saver» (и при желании «Family»).
- По клику — диспатчить в state соответствующие проценты (через существующий reducer или отдельный action `SET_PRESET`).
- Слайдеры анимированно переходят к новым значениям (уже есть анимация Recharts/Radix или добавить transition).

**Где менять:** `App.tsx` (кнопки), `budgetReducer.ts` или `budgetActions.ts` (preset values), при необходимости `constants/budget.ts` (константы пресетов).

**Промпт для агента:** *"Add 3 preset buttons above the chart: 'Student', 'Balanced', 'Aggressive Saver'. Clicking them should animate the sliders to specific values."*

**Приоритет:** 🔴 Высокий
**Статус:** ❌ Не реализовано

---

#### 💾 [FEATURE] «Запомни меня» (Local Storage)
**Описание:** При перезагрузке страницы доход и проценты сбрасываются. Нужно сохранять ввод пользователя и позиции слайдеров в Local Storage и восстанавливать при загрузке.

**Что сделать:**
- При изменении `monthlyIncome` или `percentages` — сохранять в `localStorage` (ключ, например, `smart-salary-slicer-state`).
- При инициализации приложения (в `useBudget` или в корне App) — читать из `localStorage` и, если данные валидны, использовать как начальный state вместо `INITIAL_STATE`.
- Учесть: сумма процентов должна быть 100%; при загрузке «битых» данных — fallback на дефолт.

**Где менять:** `useBudget.ts` (initial state из localStorage), при монтировании/обновлении state — запись в localStorage (useEffect при изменении state).

**Промпт для агента:** *"Save the user's input and slider positions to LocalStorage so data persists after refreshing the page."*

**Приоритет:** 🔴 Высокий
**Статус:** ❌ Не реализовано

---

#### 🎯 [FEATURE] Калькулятор цели (Goal Tracker)
**Описание:** Пользователь видит, сколько откладывает (например, 20%). Добавить блок: «На что копим?» + «Цена». Приложение считает: «При такой зарплате и таких сбережениях ты купишь это через N месяцев».

**Что сделать:**
- Секция «Goal»: поле «Target Name» (название цели), поле «Target Price» (цена в валюте).
- Расчёт: `months = ceil(Target Price / monthlySavings)`, где `monthlySavings = monthlyIncome * (savings% / 100)`.
- Вывод: «You’ll reach this goal in N months» (или «in N years M months» если N > 12).
- Учесть edge cases: нулевые сбережения, нулевая цена.

**Где менять:** новый компонент `GoalTracker.tsx` или секция в `App.tsx`, использовать `amounts.savings` из `useBudgetCalculations`.

**Промпт для агента:** *"Add a 'Goal' section. Input: 'Target Name' and 'Target Price'. Output: Calculate how many months it will take to afford it based on the current 'Savings' amount."*

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не реализовано

---

### 2. UX / Удобство

#### Красивый ввод денег (форматирование при вводе)
**Описание:** При вводе `100000` автоматически показывать разделители тысяч и символ валюты, например `100 000 ₽` или `100,000 $`, чтобы выглядело профессионально.

**Что сделать:**
- В `IncomeInput` (и при желании в Goal «Target Price») форматировать значение при отображении: пробелы или запятые как разделители тысяч + символ валюты.
- Уже есть `formatInputValue` / `formatCurrency` в `formatters.ts` — расширить или использовать при отображении, не ломая ввод (cursor position, только цифры при onChange).

**Промпт для агента:** *"Format the input field to automatically add thousand separators (spaces) and a currency symbol as the user types."*

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не исправлено

---

#### Мобильная адаптивность
**Описание:** Критично, чтобы на телефоне всё читалось и было удобно тапать.

**Что сделать:**
- Проверить все экраны на ширине ~375px и ~414px.
- График и карточки — вертикально (stack), без горизонтального скролла.
- Поля ввода и кнопки — достаточно большие для touch (min-height ~44px), отступы не слишком мелкие.

**Промпт для агента:** *"Ensure the layout is fully responsive. On mobile screens, stack the chart and the cards vertically, and make input fields large enough for touch."*

**Приоритет:** 🔴 Высокий
**Статус:** ❌ Не исправлено

---

#### Тема Dark / Light (переключатель)
**Описание:** Если ещё нет переключателя — добавить. Стандарт: тёмная тема по умолчанию, опция светлой.

**Что сделать:**
- Переключатель (toggle или кнопка) в header или в настройках.
- Состояние темы: CSS-класс на `html` (например `dark` / `light`) или `data-theme`, стили через Tailwind `dark:`.
- При желании сохранять выбор в Local Storage.

**Промпт для агента:** *"Implement a toggle for Dark/Light mode using Tailwind classes. Default to Dark mode."*

**Приоритет:** 🟡 Средний
**Статус:** ❌ Не реализовано

---

### 3. Killer Feature для демо

#### 📸 [FEATURE] Экспорт в картинку («Похвастаться бюджетом»)
**Описание:** Кнопка «Share My Plan»: сгенерировать красивую картинку с диаграммой и сводкой по категориям, чтобы можно было скачать или отправить (жене/мужу, в сториз).

**Что сделать:**
- Кнопка «Share» / «Share My Plan» в интерфейсе.
- По клику — скриншот области с графиком и карточками (или только график + Total + категории) через библиотеку вроде `html2canvas` или `dom-to-image`.
- Скачивание как PNG (или выбор «Скачать» / «Скопировать»).
- Учесть качество (retina), фон и читаемость текста на изображении.

**Где менять:** новый хелпер/хук для capture, кнопка в Layout или над графиком. Зависимость: `html2canvas` или аналог.

**Промпт для агента:** *"Add a 'Share' button. When clicked, use a library like html2canvas to take a screenshot of the chart and summary cards, and let me download it as an image."*

**Приоритет:** 🔵 Низкий (вау-эффект)
**Статус:** ❌ Не реализовано

---

### Рекомендуемый порядок (из Части 5)
1. **Сначала:** Пресеты + Local Storage (быстро, сразу делает приложение «умным»).
2. **Потом:** Goal Tracker, форматирование ввода, мобильная адаптивность, Dark/Light toggle.
3. **По времени:** Share/Export в картинку.

---

## 📊 Статистика TODO Checklist

| Категория | Всего задач | Выполнено | В процессе | Не начато |
|-----------|-------------|------------|-------------|------------|
| 🏗️ Базовая инфраструктура | 4 | 0 | 0 | 4 |
| 🎨 Layout компоненты | 2 | 0 | 0 | 2 |
| 🧩 UI компоненты | 4 | 0 | 0 | 4 |
| 📊 Charts визуализация | 1 | 0 | 0 | 1 |
| 🔧 Интеграция | 2 | 0 | 0 | 2 |
| 📚 Документация | 1 | 0 | 0 | 1 |
| **ИТОГО** | **14** | **0** | **0** | **14** |

**Прогресс UI разработки:** 0% (бизнес-логика готова, UI не начата)

---

## 📊 Статистика Code Review

| Категория | Всего | Исправлено | В процессе |
|-----------|--------|------------|-------------|
| 🟡 Высокий приоритет (Часть 2) | 6 | 0 | 0 |
| 🔵 Низкий приоритет (Часть 2) | 1 | 0 | 0 |
| 🐛 Баги из UI (Часть 3) | 4 | 0 | 0 |
| 🎯 Feature requests (Часть 4) | 1 | 0 | 0 |
| 🚀 Real Life Features (Часть 5) | 7 | 0 | 0 |
| **ИТОГО** | **19** | **0** | **0** |

---

## 🎯 Рекомендуемый порядок разработки

### Фаза 1: Фундамент (ОБЯЗАТЕЛЬНО)
1. ✅ Создать `src/globals.css` - включить Tailwind
2. ✅ Создать `src/main.tsx` - entry point
3. ✅ Создать `src/App.tsx` - главный компонент
4. ✅ Создать `tsconfig.json` - path aliases для комфортной разработки

### Фаза 2: Layout
5. ✅ Создать `src/components/layout/Layout.tsx`
6. ✅ Создать `src/components/layout/Header.tsx`

### Фаза 3: UI компоненты (в любом порядке)
7. ✅ Создать `src/components/ui/IncomeInput.tsx`
8. ✅ Создать `src/components/ui/CategoryCard.tsx`
9. ✅ Создать `src/components/ui/DailySafeToSpendCard.tsx`
10. ✅ Создать `src/components/ui/PercentageSlider.tsx`

### Фаза 4: Visualization
11. ✅ Создать `src/components/charts/BudgetDonutChart.tsx`

### Фаза 5: Интеграция
12. ✅ Собрать все компоненты в `App.tsx`
13. ✅ Протестировать state flow
14. ✅ Протестировать все edge cases

### Фаза 6: Code Review (можно параллельно с Фазой 5)
15. ✅ Исправить дублирование логики валидации
16. ✅ Определиться с шрифтами и применить согласованно
17. ✅ Улучшить fallback логику (пропорциональное распределение)
18. ✅ Добавить path aliases в tsconfig.json
19. ✅ Исправить иконки в CATEGORY_CONFIG
20. ✅ Добавить валидацию отрицательных чисел в formatInputValue
21. ✅ Проверить и уточнить цвета по дизайн-системе
22. ✅ Создать globals.css с анимациями

### Фаза 7: Финализация
23. ✅ Создать README.md
24. ✅ Final review и cleanup
25. ✅ Deploy preview / screenshot

### Фаза 8: Real Life Features (Часть 5 — после хакатона)
26. ✅ Пресеты (Student / Balanced / Aggressive Saver) + Local Storage
27. ✅ Goal Tracker, форматирование ввода, мобильная адаптивность, Dark/Light
28. ✅ Share / экспорт в картинку (по времени)

---

## 📝 Для другого агента

### Как использовать этот файл:
1. ✅ Отмечайте задачи выполненными, когда закончили
2. 🔄 Используйте "В процессе" когда работаете над задачей
3. ❌ Оставляйте "Не начато" если еще не приступали
4. При завершении Фазы 1-5, переходите к Фазе 6-7
5. Учитывайте что бизнес-логика уже готова - фокус на UI

### Интеграция с существующим кодом:
- **Типы:** `src/types/budget.ts` - готовы, используйте
- **State:** `src/state/budgetReducer.ts` + `budgetActions.ts` - готовы
- **Hooks:** `src/hooks/useBudget.ts` + `useBudgetCalculations.ts` - готовы
- **Utils:** `src/utils/formatters.ts` + `percentageValidator.ts` - готовы
- **Constants:** `src/constants/budget.ts` - готовы

**Вам нужно только:** Создать UI компоненты и соединить всё в App.tsx!

---

**Примечание:** Этот файл адаптирован под реалии проекта в разработке. Вместо списка "критических ошибок" это TODO Checklist для последовательной разработки + Code Review для улучшения существующего кода.
